# Galería — behaviors

- Each photo is an interactive source-linked gallery item. The clone opens the local high-resolution photo in an accessible lightbox overlay with close button and Escape support.
- CTA navigates to `/admisiones/`.
- Shared header, mobile menu, footer and WhatsApp launcher remain interactive.
- The source admin bar is omitted.
