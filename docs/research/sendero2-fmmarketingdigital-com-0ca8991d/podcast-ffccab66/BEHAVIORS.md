# Podcast — behaviors

- Clicking a preview replaces the thumbnail with the corresponding YouTube embed, matching the source’s lazy video interaction.
- Episode 1 has no available source thumbnail and intentionally uses the same neutral placeholder appearance; episode 5 is also initially neutral while lazy media is not loaded.
- Shared header/mobile menu/footer/WhatsApp launcher remain interactive.
- The source admin bar is omitted.
